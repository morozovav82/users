apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: users-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
  - host: arch.homework
    http:
      paths:
        - path: /otusapp/morozovav($|/)(.*)
          backend:
            serviceName: users-service
            servicePort: 9000
